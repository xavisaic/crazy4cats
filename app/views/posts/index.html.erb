<p style="color: green"><%= notice %></p>


<a class="btn btn-success" <%= link_to "New post", new_post_path %></a>








<div class="container">
  <h1>Tu Timeline</h1>
  <% @posts.each do |post| %>
    <div class="post-card mt-4 border p-3">

      <!-- Información de la publicación -->
      <div class="post-info">
        <p class="post-user"><strong><%= post.user.email %></strong></p>
        <p class="post-content"><%= post.content %></p>
      </div>
    
      <!-- Imagen del perfil del usuario -->
      <img class="timeline-post-avatar" src="https://picsum.photos/200/200" alt="Perfil de usuario">
      
      <div class="timeline-post-actions mt-2 mb-2">
        <a class="btn btn-primary" href="/likes/1/like">Me gusta (<%= post.likes.count %>)</a>
        <a class="btn btn-success" <%= link_to "Show this post", post %></a>
      </div>

      <!-- Área para agregar comentarios -->
      <div class="comments-section">
        <!-- Formulario para agregar un comentario -->
        <%= form_for [post, Comment.new] do |f| %>
          <div class="form-group">
            <%= f.text_area :content, placeholder: "Escribe tu comentario aquí...", class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.submit "Comentar", class: "btn btn-primary" %>
          </div>
        <% end %>
        
        <!-- Mostrar los comentarios existentes -->
        <% post.comments.each do |comment| %>
          <div class="comment">
            <strong><%= comment.user.email %></strong>
            <p><%= comment.content %></p>
          </div>
        <% end %>
      </div>
      

    </div>
    <br>
    <br>
    <br>
  <% end %>

</div>



<footer>
  <p>Copyright &copy; 2023</p>
</footer>



